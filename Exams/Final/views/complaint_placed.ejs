<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Complaint Placed</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card p-4">
          <h3 class="text-center mb-4 text-success">Complaint Submitted Successfully</h3>
          <div class="mb-3">
            <strong>Order ID:</strong> <%= complaint.orderId %>
          </div>
          <div class="mb-3">
            <strong>Complaint Message:</strong>
            <div class="border rounded p-2 bg-light"><%= complaint.message %></div>
          </div>
          <div class="mb-3">
            <strong>Submitted At:</strong> <%= complaint.timestamp.toLocaleString() %>
          </div>
          <% if (complaint.user && complaint.user.name) { %>
            <div class="mb-3">
              <strong>User:</strong> <%= complaint.user.name %> (<%= complaint.user.email %>)
            </div>
          <% } %>
          <div class="text-center mt-4">
            <a href="/" class="btn btn-primary">Back to Home</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html> 